#include<stdio.h>
int main()
{
        int r=3;
        int height=7;
        printf("the area of a circle with radius %d is %f\n",r,3.14*r*r);
        printf("the volume of a cylinder with radius %d and height %d is %f",r,height,3.14*r*r*height);
        return 0;
}